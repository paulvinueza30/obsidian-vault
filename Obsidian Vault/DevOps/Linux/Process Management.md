CPU algo thinks whatever proc takes up most time on cpu should have highest prio


# Services

procs that run in the background

start at the boot of the os

procs can be converted into services

# Monitoring

a bunch of commands to check disk. cpu, memory usage

but standard now is use monitoring tools like grafana , or cloud provider tools for monitoring
