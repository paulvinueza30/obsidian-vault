Power On -> BIOS -> Master Boot Record -> Boot Loader -> Kernel (Linux OS) -> Initial RAM disk -> /sbin/init (parent process) -> Command Shell using getty -> X Windows System

sbin/init : Starts other process to get the system running